{
  "name": "todolist",
  "version": "1.0.0",
  "main": "index.js",
  
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx webpack build",
    "watch": "npx webpack build --watch",
    "gh-pages-deploy": "npm run build && git add . && git commit -m 'Deploy to gh-pages' && git subtree push --prefix dist origin gh-pages"
  },
  "author": "Sparsh",
  "license": "ISC",
  "description": "This is a simple todo list app using webpack under guidance of theodinproject",
  "devDependencies": {
    "css-loader": "^6.10.0",
    "html-webpack-plugin": "^5.6.0",
    "style-loader": "^3.3.4",
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4"
  },
  "dependencies": {
    "webpack-merge": "^5.10.0"
  }
}
